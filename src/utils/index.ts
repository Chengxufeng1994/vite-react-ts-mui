import resetApp from './resetApp';
import toCapitalize from './toCapitalize';
import welcome from './welcome';

export default { resetApp, toCapitalize, welcome };
export { resetApp, toCapitalize, welcome };
